from setuptools import setup

setup(
    name='json_ref_depth',
    version='0.1',
    description='Recursively replaces Json schema references with the referenced content to a specific level.',
    url='tbd',
    author='Jonny Fuller',
    author_email='mr.waffles@gmail.com',
    py_modules=['json_ref_depth'],
    license='MIT',
    keywords='jsonschema'
)
